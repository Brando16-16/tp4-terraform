resource "aws_vpc" "vpc_<###ITEM_NAME###>" {
  cidr_block           = "<###PARAM_1###>"
  enable_dns_support   = true
  enable_dns_hostnames = true

  tags = {
    Name = "${var.project_name}-vpc-<###ITEM_NAME###>"
  }
}
